---
/**
 * ListComp - A horizontal statistics/list section.
 * Ported from image showing 4 key metrics with dividers.
 */
const stats = [
    { value: "$500B+", label: "Invoices processed" },
    { value: "50+", label: "Countries covered" },
    { value: "1B+", label: "E-Invoices processed" },
    { value: "5000+", label: "Enterprise clients" },
];
---

<section class="list-comp pb-25 bg-[#FFFFFF]!">
    <div
        class="mx-auto max-w-[var(--container-max)] px-4 lg:px-6 pt-12"
    >
        <div class="flex items-center gap-4 mb-12 select-none">
            <span class="text-xs font-normal tracking-widest leading-1.5 text-[#000000]/68 uppercase shrink-0">
                Built for enterprise scale
            </span>
            <div class="h-[1px] flex-grow bg-black/10"></div>
        </div>
        <div
            class="flex flex-wrap md:flex-nowrap justify-between items-center gap-8 md:gap-12"
        >
            {
                stats.map((stat, index) => (
                    <>
                        <div
                            class="flex-1 min-w-[140px] md:min-w-0 text-start px-6 premium-reveal"
                            style={`transition-delay: ${index * 100}ms`}
                        >
                            <div class="text-[22px] font-medium text-black/80 text-center mb-3 leading-tight tracking-tight">
                                {stat.value}
                            </div>
                            <p class="text-sm lg:text-base font-normal text-[#000000]/68 text-center text-balance leading-relaxed">
                                {stat.label}
                            </p>
                        </div>
                        {index < stats.length - 1 && (
                            <div class="hidden md:block w-[1px] h-16 bg-[#1E1256]/10 shrink-0" />
                        )}
                    </>
                ))
            }
        </div>
    </div>
</section>

<style is:global>
    .list-comp {
        /* Subtle inner shadow for depth */
        box-shadow: inset 0 2px 40px rgba(0, 0, 0, 0.02);
    }
</style>
