<section id="why-cleartax" class="bg-[#FFFFFF] py-12 lg:py-25 w-full">
  <div class="max-w-[var(--container-max)] mx-auto lg:px-6 px-4">
    <div class="text-center">
      <h2
        id="oman-title-shield"
        class="text-[26px] sm:text-[32px] max-w-148.5 lg:text-[44px] font-semibold text-black leading-[1.18] m-0"
      >
        Built for the long game, <span class="text-[#655DFF]">
          not just the deadline</span
        >
      </h2>
      <p
        id="oman-subtitle-shield"
        class="lg:text-lg! text-base! font-normal! text-black/80! mt-4! premium-reveal [transition-delay:150ms]"
      >
        One in three companies switches their e-invoicing vendor within a year.
        ClearTax is designed to be the last compliance platform you need..
      </p>
    </div>

    <div class="grid grid-cols-12 gap-6 mt-12 w-full">
      <!-- Approved PA Registry -->
      <div
        class="col-span-6 max-lg:col-span-12 tilted-card min-h-[300px] premium-reveal"
      >
        <div
          class="tilted-card-inner bg-[#FDF2ED] rounded-[16px] p-5 flex flex-col justify-between overflow-hidden relative h-full"
        >
          <div class="relative z-10 mb-10">
            <h3
              class="text-[22px] leading-[1.36] font-medium text-black m-0 mb-3"
            >
              Approved PA Registry
            </h3>
            <p
              class="text-base max-w-124 font-normal leading-normal text-black/80 m-0"
            >
              Listed as a Partenaire Dématérialisateur and already tested in the
              French PPF environment since the February 2026 pilot.
            </p>
          </div>
          <div
            class="px-5 flex justify-center items-end -mb-8 max-w-132.5 w-full mx-auto tilted-card-img-wrapper"
          >
            <img
              src="/images/france-product/platform-advantage-1.png"
              alt="Speedometer"
              class="w-full h-auto object-contain"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>

      <!-- Connects to Everything (6 columns) -->
      <div
        class="col-span-6 max-lg:col-span-12 tilted-card min-h-[300px] premium-reveal"
      >
        <div
          class="tilted-card-inner bg-[#EDEBFA] rounded-[16px] flex flex-col justify-end overflow-hidden relative h-full"
        >
          <div
            class="absolute top-[10%] right-0 max-w-93 aspect-372/176 mx-auto w-full tilted-card-img-wrapper"
          >
            <img
              src="/images/france-product/platform-advantage-2.png"
              alt="Connectors"
              class="w-full h-auto object-contain"
              loading="lazy"
              decoding="async"
            />
          </div>
          <div class="relative z-10 p-5 bottom-0">
            <h3
              class="text-[22px] leading-[1.36] font-medium text-black m-0 mb-3"
            >
              No ERP Rework
            </h3>
            <p class="text-base font-normal leading-normal text-black/80 m-0">
              Regulatory amendments, new fields, and DGFiP changes are absorbed
              in the cloud layer. Your IT team connects once.
            </p>
          </div>
        </div>
      </div>

      <!-- Always-On e-Invoicing (5 columns) -->
      <div
        class="col-span-7 max-lg:col-span-12 tilted-card min-h-[300px] premium-reveal"
      >
        <div
          class="tilted-card-inner bg-[#E8F5F7] rounded-[16px] flex gap-6.25 overflow-hidden relative h-full"
        >
          <div class="relative z-10 p-5">
            <h3
              class="text-[22px] leading-[1.36] max-w-36.25 font-medium text-black m-0 mb-3"
            >
              In-House Tax Expertise
            </h3>
            <p class="font-normal max-w-78 leading-normal text-black/80 m-0">
              French tax experts, compliance specialists, and 1,000+ engineers
              monitor DGFiP updates and ship platform changes early.
            </p>
          </div>
          <div
            class="flex pb-4 max-w-85.5 aspect-342/240 w-full tilted-card-img-wrapper mt-20"
          >
            <img
              src="/images/france-product/platform-advantage-3.png"
              alt="Always-On"
              class="w-full h-auto object-contain"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>

      <!-- Global Mandate Experience -->
      <div
        class="col-span-5 max-lg:col-span-12 tilted-card min-h-[300px] premium-reveal"
      >
        <div
          class="tilted-card-inner bg-[ECF7ED] rounded-[16px] flex items-end overflow-hidden relative h-full"
        >
          <!-- shadow layer -->
          <div
            class="w-full h-[85%] z-10 absolute bottom-[0%] " >
            <img
              src="/images/france-product/blur.png"
              alt="Zero Downtime"
              class="w-full h-auto "
              loading="lazy"
              decoding="async"
            />
          </div>
          <div class=" relative z-200 p-5">
            <h3
              class="text-[22px] leading-[1.36] font-medium text-black m-0 mb-3"
            >
              Global Mandate Experience
            </h3>
            <p class="text-base font-normal leading-normal text-black/80 m-0">
              100+ European clients already run on ClearTax, giving France
              onboarding a proven and repeatable implementation model.
            </p>
          </div>
          <div
            class="absolute top-[-31%]  w-[480px] aspect-square scale-[0.9]  inset-0 tilted-card-img-wrapper z-5"
          >
            <img
              src="/images/france-product/platform-advantage-4.png"
              alt="Zero Downtime"
              class="w-full h-auto object-contain"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>

      <!--Enterprise-Scale Performance -->
      <div
        class="col-span-5 max-lg:col-span-12 tilted-card min-h-[300px] premium-reveal"
      >
        <div
          class="tilted-card-inner bg-[#D8EFF2] rounded-[16px] p-5 gap-8 flex flex-col lg:flex-row overflow-hidden relative h-full"
        >
          <div
            class="flex flex-col items-end order-2 lg:order-1 relative z-10"
          >
            <h3
              class="text-[22px] leading-[1.36] font-medium text-black m-0 mb-3 w-full"
            >
              European Client Playbook
            </h3>
            <p
              class="text-base max-w-64 font-normal leading-normal text-black/80 m-0 w-full"
            >
              100+ European clients already run on ClearTax,
 giving France onboarding a proven and repeatable implementation model.
            </p>
          </div>
          <div
            class="max-w-[406px] w-full flex items-start justify-end order-1 lg:order-2 ml-auto tilted-card-img-wrapper"
          >
            <img
              src="https://cdn.prod.website-files.com/69fda2cd307dcc6cf810e935/6a1e7275576b92ed6daf29f0_img-5.png"
              alt="VAT Assurance"
              class="w-full h-auto object-contain origin-top-right flex justify-center items-end pt-3"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>

      <!-- Enterprise AI Security (5 columns) -->
      <div
        class="col-span-7 max-lg:col-span-12 tilted-card min-h-[300px] premium-reveal"
      >
        <div
          class="tilted-card-inner bg-[#FDF2ED] rounded-[16px] p-5 flex flex-col justify-between overflow-hidden relative h-full"
        >
          <div class="mb-8 relative z-10">
            <h3
              class="text-[22px] leading-[1.36] font-medium text-black m-0 mb-3"
            >
              Penalties
            </h3>
            <p
              class="text-base font-normal max-w-99 leading-normal text-black/80 m-0"
            >
              Penalties applied per regulations for non-compliance. A grace
              period is available before full enforcement begins.
            </p>
          </div>
          <div
            class="mt-auto flex justify-center items-end pt-4 max-w-[366px] w-full mx-auto tilted-card-img-wrapper"
          >
            <img
              src="https://cdn.prod.website-files.com/69fda2cd307dcc6cf810e935/6a1e727cf0513cfd007535a2_img-6.png"
              alt="Security"
              class="w-full h-auto object-contain"
              loading="lazy"
              decoding="async"
            />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style is:global>
  /* Tilted Card Styles */
  .tilted-card {
    perspective: 1000px;
    z-index: 1;
  }
  .tilted-card-inner {
    transform-style: preserve-3d;
    will-change: transform;
    transition:
      transform 0.1s ease-out,
      box-shadow 0.3s ease; /* Fallback */
  }
  .tilted-card-img-wrapper {
    transform: translateZ(20px);
    transform-style: preserve-3d;
    backface-visibility: hidden;
  }
  .tilted-card-img-wrapper img {
    transform: translateZ(10px);
  }

  /* WEBFLOW FIX: Shielding the titles against global H2/P overrides */
  #usp-title-shield {
    font-size: 44px !important;
    line-height: 1.18 !important;
    font-weight: 600 !important;
    color: black !important;
    text-align: center !important;
    margin: 0 !important;
  }
  #usp-subtitle-shield {
    font-size: 18px !important;
    font-weight: 500 !important;
    color: black !important;
    opacity: 0.8 !important;
    text-align: center !important;
    margin-top: 16px !important;
  }
  @media (max-width: 1024px) {
    #usp-title-shield {
      font-size: 32px !important;
    }
  }
  @media (max-width: 640px) {
    #usp-title-shield {
      font-size: 26px !important;
    }
    #usp-subtitle-shield {
      font-size: 16px !important;
    }
  }
</style>

<script is:inline>
  (function () {
    let initialized = false;
    const section = document.getElementById("why-cleartax");

    function initTiltedCards() {
      if (initialized) return;
      initialized = true;

      const cards = document.querySelectorAll(".tilted-card");
      cards.forEach((card) => {
        const inner = card.querySelector(".tilted-card-inner");
        if (!inner) return;

        const rotateAmplitude = 12;
        const scaleOnHover = 1.02;

        let targetRotateX = 0,
          targetRotateY = 0,
          targetScale = 1;
        let currentRotateX = 0,
          currentRotateY = 0,
          currentScale = 1;
        let rafId = null;

        function update() {
          currentRotateX += (targetRotateX - currentRotateX) * 0.1;
          currentRotateY += (targetRotateY - currentRotateY) * 0.1;
          currentScale += (targetScale - currentScale) * 0.1;
          inner.style.transform = `rotateX(${currentRotateX}deg) rotateY(${currentRotateY}deg) scale(${currentScale})`;
          if (
            Math.abs(targetRotateX - currentRotateX) > 0.01 ||
            Math.abs(targetRotateY - currentRotateY) > 0.01 ||
            Math.abs(targetScale - currentScale) > 0.001
          ) {
            rafId = requestAnimationFrame(update);
          } else {
            rafId = null;
          }
        }

        card.addEventListener("mousemove", (e) => {
          const rect = card.getBoundingClientRect();
          const offsetX = e.clientX - rect.left - rect.width / 2;
          const offsetY = e.clientY - rect.top - rect.height / 2;
          targetRotateX = (offsetY / (rect.height / 2)) * -rotateAmplitude;
          targetRotateY = (offsetX / (rect.width / 2)) * rotateAmplitude;
          targetScale = scaleOnHover;
          if (!rafId) rafId = requestAnimationFrame(update);
        });

        card.addEventListener("mouseenter", () => {
          targetScale = scaleOnHover;
          if (!rafId) rafId = requestAnimationFrame(update);
        });

        card.addEventListener("mouseleave", () => {
          targetRotateX = 0;
          targetRotateY = 0;
          targetScale = 1;
          if (!rafId) rafId = requestAnimationFrame(update);
        });
      });
    }

    if (section) {
      section.addEventListener("mouseenter", initTiltedCards, {
        once: true,
      });
      section.addEventListener("touchstart", initTiltedCards, {
        once: true,
      });
      if ("IntersectionObserver" in window) {
        const obs = new IntersectionObserver(
          (entries) => {
            if (entries[0].isIntersecting) {
              initTiltedCards();
              obs.disconnect();
            }
          },
          { rootMargin: "100px" },
        );
        obs.observe(section);
      } else {
        initTiltedCards();
      }
    }
  })();
</script>
