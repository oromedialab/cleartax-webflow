{
  "name": "cleartax-webflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "node scripts/build-css.mjs --gen-only",
    "dev": "astro dev",
    "prebuild": "node scripts/build-css.mjs --gen-only",
    "build": "npm run build:css && astro build && node scripts/extract-embeds.mjs && node scripts/assemble-confirmations.mjs",
    "build:css": "node scripts/build-css.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^4.16.19",
    "tailwind": "^4.0.0"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.2.4",
    "@tailwindcss/vite": "^4.2.4",
    "concurrently": "^9.0.0",
    "css-tree": "^3.2.1",
    "node-html-parser": "^6.1.13"
  }
}
